apiVersion: 1

datasources:
  - name: RGS Prometheus
    uid: rgs-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
      manageAlerts: false
      prometheusType: Prometheus
      prometheusVersion: "3.0.0"
      timeInterval: 15s

# Grafana 只通过内部 observability 网络访问 Prometheus；禁止浏览器直连数据源。
# English: Grafana only accesses Prometheus through the internal observability network; the browser is
# prohibited from directly connecting to the data source.
